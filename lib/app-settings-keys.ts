// QUOTE-LABOUR — app_settings keys. Kept in a plain module (not the
// "use server" actions file, which may only export async functions) so both
// server actions and client components can import the key constant.

export const DEFAULT_LABOUR_SELL_RATE_KEY = "default_labour_sell_rate";
